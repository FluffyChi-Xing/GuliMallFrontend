export namespace ArgumentTypes {
    export interface categoryLabelTypes {
        label: string;
        prop: string;
        width?: string;
        overflows?: boolean;
    }

    export interface categoryDataTypes {
        id: string;
        name: string;
        logo: string;
        desc: string;
        status: boolean;
        initial: string;
        sort: string;
    }
}
